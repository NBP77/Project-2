<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Groupie</title>
  <link rel="stylesheet" type="text/css" href="/css/jass.css">
  <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>

<body>
  <header>
    <h1>Groupie</h1>
  </header>
  <nav>
    <a href="/">Home</a>
    <a class="link" href="/">Profile</a>
    {{#if loggedIn}}
    <button id="logout" class="btn-no-style">Logout</button>
    {{else}}
    <a href="/login">Login</a>
    {{/if}}
    <a href="/">Events</a>
  </nav>
   <nav>
    {{#if loggedIn}}
    <button id="create-event" class="btn-no-style"><a href="/">Create A Event!</a></button>
    {{/if}}
  </nav>
  <main>
    <div class="gallery">
      {{{body}}}
    </div>
  </main>
  {{#if loggedIn}}
  <script src="/js/logout.js"></script>
  
  {{/if}}
</body>

</html>