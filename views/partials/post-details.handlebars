<div class="card">
    <div class="card-header">
       <h4>
         <a class="link" href="/event/{{id}}">{{title}}</a>
         </h4>
        <p>
          Event by {{user.username}} on {{format_date created_at}}
        </p>
    </div>
        <p class="card-body">{{content}}</p>
</div>